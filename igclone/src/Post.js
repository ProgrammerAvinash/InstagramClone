import React from 'react';

const Post = () => {
    return (
        <div className="post">
        {`Header = > username + Avatar`}
        {`pic`}
        {`Avatar => `}            
        </div>
    );
};

export default Post;